<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="msa.userservice.adapter.out.persistence.mybatis.MemberMapper">

    <select id="findByLoginId" resultType="msa.userservice.domain.Member">
        SELECT
            member_id,
            member_type,
            login_id,
            password,
            name,
            email,
            phone_no,
            company_name,
            biz_no,
            use_yn,
            last_login_dt,
            reg_dt,
            mod_dt
        FROM tb_member
        WHERE login_id = #{loginId}
    </select>

    <insert id="save" parameterType="msa.userservice.domain.Member">
        <selectKey keyProperty="memberId" resultType="long" order="AFTER">
            SELECT currval(pg_get_serial_sequence('tb_member', 'member_id'))
        </selectKey>
        INSERT INTO tb_member (
        member_type,
        login_id,
        password,
        name,
        email,
        phone_no,
        company_name,
        biz_no,
        use_yn,
        reg_dt,
        mod_dt
        ) VALUES (
        #{memberType},
        #{loginId},
        #{password},
        #{name},
        #{email},
        #{phoneNo},
        #{companyName},
        #{bizNo},
        #{useYn},
        #{regDt},
        #{modDt}
        )
    </insert>

</mapper>
